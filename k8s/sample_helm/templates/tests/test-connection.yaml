apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sample_helm.fullname" . }}-test-connection"
  labels:
{{ include "sample_helm.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "sample_helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
